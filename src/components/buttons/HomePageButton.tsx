import { Button } from 'react-bootstrap';

export default function HomePageButton() {
    return (
        <Button variant="primary" href="/">
            Return to home page
        </Button>
    );
}